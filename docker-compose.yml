version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./frontend:/app/../frontend
      - backend-data:/app/data
    env_file:
      - ./backend/.env
    restart: unless-stopped

volumes:
  backend-data:
