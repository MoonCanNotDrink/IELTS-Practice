{
  "scripts": {
    "test": "cd backend && (python -m unittest discover -p 'test*_route*.py' -v || python3 -m unittest discover -p 'test*_route*.py' -v) && cd .. && npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
