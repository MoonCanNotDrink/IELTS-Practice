{
  "scripts": {
    "test": "cd backend && (python -m unittest test_tts_route test_free_practice_routes -v || python3 -m unittest test_tts_route test_free_practice_routes -v) && cd .. && npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
