{
  "scripts": {
    "test": "cd backend && (python -m unittest test_tts_route -v || python3 -m unittest test_tts_route -v) && cd .. && npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
